<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{firstName}} {{lastName}}</title>
  <style>
    /* Base styles */
    @page {
      size: A4;
      margin: 0;
    }
    
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      line-height: 1.5;
      color: #333;
      margin: 0;
      padding: 0;
      background-color: white;
    }
    
    .resume {
      width: 210mm;
      min-height: 297mm;
      margin: 0;
      padding: 0;
      background-color: white;
      box-sizing: border-box;
      box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
      border-radius: 0.375rem;
      overflow: hidden;
    }
    
    /* Header section */
    .header {
      padding: 1.5rem;
      color: white;
      position: relative;
      background-color: {{themeColor}};
    }
    
    .header-decoration-1 {
      position: absolute;
      top: 0;
      right: 0;
      width: 10rem;
      height: 10rem;
      border-radius: 9999px;
      background-color: rgba(255, 255, 255, 0.1);
    }
    
    .header-decoration-2 {
      position: absolute;
      bottom: 0;
      left: 0;
      width: 15rem;
      height: 5rem;
      border-radius: 9999px;
      background-color: rgba(255, 255, 255, 0.05);
    }
    
    .header-content {
      position: relative;
      z-index: 10;
    }
    
    .name {
      font-size: 1.875rem;
      font-weight: bold;
      letter-spacing: -0.025em;
      margin: 0;
    }
    
    .job-title {
      font-size: 1.25rem;
      margin-top: 0.25rem;
      font-weight: 300;
      letter-spacing: 0.025em;
      opacity: 0.9;
    }
    
    /* Contact strip */
    .contact-strip {
      background-color: #f3f4f6;
      padding: 0.75rem;
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 1rem;
      font-size: 0.875rem;
      border-bottom: 1px solid #e5e7eb;
    }
    
    .contact-item {
      display: flex;
      align-items: center;
    }
    
    .contact-icon {
      width: 1rem;
      height: 1rem;
      margin-right: 0.375rem;
    }
    
    .contact-link {
      color: {{themeColor}};
      text-decoration: none;
    }
    
    .contact-link:hover {
      text-decoration: underline;
    }
    
    /* Main content */
    .main-content {
      display: flex;
      flex-direction: column;
    }
    
    @media (min-width: 768px) {
      .main-content {
        flex-direction: row;
      }
    }
    
    .main-column {
      padding: 1.25rem;
      width: 100%;
    }
    
    @media (min-width: 768px) {
      .main-column {
        width: 66.666667%;
      }
    }
    
    .side-column {
      padding: 1.25rem;
      width: 100%;
      background-color: #f9fafb;
    }
    
    @media (min-width: 768px) {
      .side-column {
        width: 33.333333%;
      }
    }
    
    /* Section styling */
    .section {
      margin-bottom: 1.5rem;
    }
    
    .section-title {
      font-size: 1.125rem;
      font-weight: bold;
      margin-bottom: 0.5rem;
      padding-bottom: 0.25rem;
      border-bottom: 2px solid;
      border-color: rgba({{themeColorRGB}}, 0.25);
      color: {{themeColor}};
      text-transform: uppercase;
    }
    
    /* Timeline items */
    .timeline-items {
      margin-top: 0.75rem;
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }
    
    .timeline-item {
      position: relative;
      padding-left: 1.5rem;
      border-left: 2px solid rgba({{themeColorRGB}}, 0.2);
      margin-left: 0.25rem;
    }
    
    .timeline-bullet {
      position: absolute;
      top: 0.375rem;
      left: -0.25rem;
      width: 0.75rem;
      height: 0.75rem;
      border-radius: 9999px;
      background-color: {{themeColor}};
    }
    
    .item-title {
      font-size: 1rem;
      font-weight: bold;
      color: {{themeColor}};
      margin: 0 0 0.25rem 0;
    }
    
    .item-subtitle {
      font-size: 0.875rem;
      font-weight: 500;
      margin: 0 0 0.25rem 0;
    }
    
    .item-date {
      font-size: 0.75rem;
      color: #6b7280;
    }
    
    .item-flex-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 0.25rem;
    }
    
    .item-description {
      font-size: 0.875rem;
      color: #4b5563;
      margin-top: 0.25rem;
    }
    
    /* Skills */
    .skills-container {
      display: flex;
      flex-wrap: wrap;
      gap: 0.375rem;
    }
    
    .skill-tag {
      display: inline-block;
      font-size: 0.75rem;
      padding: 0.25rem 0.625rem;
      border-radius: 9999px;
      border: 1px solid;
      background-color: rgba({{themeColorRGB}}, 0.1);
      border-color: rgba({{themeColorRGB}}, 0.3);
      color: {{themeColor}};
    }
    
    /* Projects */
    .project-item {
      margin-bottom: 0.75rem;
    }
    
    .project-links {
      display: flex;
      gap: 0.75rem;
    }
    
    .project-link {
      font-size: 0.75rem;
      color: {{themeColor}};
      text-decoration: none;
      display: flex;
      align-items: center;
    }
    
    .project-link:hover {
      text-decoration: underline;
    }
    
    .project-icon {
      width: 0.75rem;
      height: 0.75rem;
      margin-right: 0.25rem;
    }
    
    .tech-tag {
      font-size: 0.75rem;
      margin-bottom: 0.25rem;
      display: inline-block;
      padding: 0 0.5rem;
      border-radius: 0.25rem;
      background-color: #f3f4f6;
      color: #4b5563;
    }
    
    .tech-tag-label {
      font-weight: 600;
    }
    
    /* Icons */
    .icon-email {
      fill: none;
      stroke: currentColor;
      stroke-width: 2;
      stroke-linecap: round;
      stroke-linejoin: round;
    }
    
    .icon-phone {
      fill: none;
      stroke: currentColor;
      stroke-width: 2;
      stroke-linecap: round;
      stroke-linejoin: round;
    }
    
    .icon-location {
      fill: none;
      stroke: currentColor;
      stroke-width: 2;
      stroke-linecap: round;
      stroke-linejoin: round;
    }
    
    .icon-linkedin {
      fill: currentColor;
    }
    
    .icon-github {
      fill: currentColor;
    }
    
    .icon-globe {
      fill: none;
      stroke: currentColor;
      stroke-width: 2;
      stroke-linecap: round;
      stroke-linejoin: round;
    }
    
    .icon-link {
      fill: none;
      stroke: currentColor;
      stroke-width: 1.5;
      stroke-linecap: round;
      stroke-linejoin: round;
    }
    
    .icon-external {
      fill: none;
      stroke: currentColor;
      stroke-width: 2;
      stroke-linecap: round;
      stroke-linejoin: round;
    }
    
    @media print {
      .resume {
        box-shadow: none;
        border-radius: 0;
      }
      
      .page-break {
        page-break-before: always;
      }
      
      .timeline-item, .project-item, .item-title, .item-subtitle {
        page-break-inside: avoid;
      }
    }
  </style>
</head>
<body>
  <div class="resume">
    <!-- Header Section - Name and Title with background color -->
    <div class="header">
      <!-- Decorative design elements -->
      <div class="header-decoration-1"></div>
      <div class="header-decoration-2"></div>
      
      <div class="header-content">
        <h1 class="name">{{firstName}} {{lastName}}</h1>
        {{#if jobTitle}}
          <h2 class="job-title">{{jobTitle}}</h2>
        {{/if}}
      </div>
    </div>
    
    <!-- Contact Information Strip -->
    <div class="contact-strip">
      {{#if email}}
        <div class="contact-item">
          <svg class="contact-icon icon-email" viewBox="0 0 24 24">
            <path d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
          </svg>
          <span>{{email}}</span>
        </div>
      {{/if}}
      
      {{#if phone}}
        <div class="contact-item">
          <svg class="contact-icon icon-phone" viewBox="0 0 24 24">
            <path d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
          </svg>
          <span>{{phone}}</span>
        </div>
      {{/if}}
      
      {{#if address}}
        <div class="contact-item">
          <svg class="contact-icon icon-location" viewBox="0 0 24 24">
            <path d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
            <path d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
          </svg>
          <span>{{address}}</span>
        </div>
      {{/if}}
      
      <!-- Social Links -->
      {{#if linkedinUrl}}
        <div class="contact-item">
          <svg class="contact-icon icon-linkedin" viewBox="0 0 24 24">
            <path d="M4.98 3.5c0 1.381-1.11 2.5-2.48 2.5s-2.48-1.119-2.48-2.5c0-1.38 1.11-2.5 2.48-2.5s2.48 1.12 2.48 2.5zm.02 4.5h-5v16h5v-16zm7.982 0h-4.968v16h4.969v-8.399c0-4.67 6.029-5.052 6.029 0v8.399h4.988v-10.131c0-7.88-8.922-7.593-11.018-3.714v-2.155z"/>
          </svg>
          <a href="{{formatUrl linkedinUrl}}" target="_blank" rel="noopener noreferrer" class="contact-link">LinkedIn</a>
        </div>
      {{/if}}
      
      {{#if githubUrl}}
        <div class="contact-item">
          <svg class="contact-icon icon-github" viewBox="0 0 24 24">
            <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
          </svg>
          <a href="{{formatUrl githubUrl}}" target="_blank" rel="noopener noreferrer" class="contact-link">GitHub</a>
        </div>
      {{/if}}
      
      {{#if portfolioUrl}}
        <div class="contact-item">
          <svg class="contact-icon icon-globe" viewBox="0 0 24 24">
            <path d="M21 12a9 9 0 01-9 9m9-9a9 9 0 00-9-9m9 9H3m9 9a9 9 0 01-9-9m9 9c1.657 0 3-4.03 3-9s-1.343-9-3-9m0 18c-1.657 0-3-4.03-3-9s1.343-9 3-9m-9 9a9 9 0 019-9" />
          </svg>
          <a href="{{formatUrl portfolioUrl}}" target="_blank" rel="noopener noreferrer" class="contact-link">Portfolio</a>
        </div>
      {{/if}}
    </div>
    
    <!-- Main Content - Two Column Layout -->
    <div class="main-content">
      <!-- Left Column - Wider for main content -->
      <div class="main-column">
        <!-- Summary Section -->
        {{#if summary}}
          <div class="section">
            <h3 class="section-title">SUMMARY</h3>
            <p class="item-description">{{summary}}</p>
          </div>
        {{/if}}
        
        <!-- Experience Section -->
        {{#if experience.length}}
          <div class="section">
            <h3 class="section-title">WORK EXPERIENCE</h3>
            
            <div class="timeline-items">
              {{#each experience}}
                <div class="timeline-item">
                  <div class="timeline-bullet"></div>
                  
                  <h4 class="item-title">{{this.title}}</h4>
                  <div class="item-flex-row">
                    <h5 class="item-subtitle">
                      {{this.companyName}}{{#if this.city}}{{#if this.companyName}}, {{/if}}{{this.city}}{{/if}}{{#if this.state}}{{#if this.city}}, {{else}}{{#if this.companyName}}, {{/if}}{{/if}}{{this.state}}{{/if}}
                    </h5>
                    <span class="item-date">
                      {{this.startDate}}{{#if this.startDate}}{{#if this.currentlyWorking}} - Present{{else}}{{#if this.endDate}} - {{this.endDate}}{{/if}}{{/if}}{{/if}}
                    </span>
                  </div>
                  
                  <div class="item-description">{{{this.workSummary}}}</div>
                </div>
              {{/each}}
            </div>
          </div>
        {{/if}}
        
        <!-- Projects Section -->
        {{#if projects.length}}
          <div class="section">
            <h3 class="section-title">PROJECTS</h3>
            
            <div class="timeline-items">
              {{#each projects}}
                <div class="project-item">
                  <div class="item-flex-row">
                    <h4 class="item-title">{{this.projectName}}</h4>
                    <div class="project-links">
                      {{#if this.githubLink}}
                        <a href="{{formatUrl this.githubLink}}" target="_blank" rel="noopener noreferrer" class="project-link">
                          <svg class="project-icon icon-github" viewBox="0 0 24 24">
                            <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
                          </svg>
                          Code
                        </a>
                      {{/if}}
                      {{#if this.deployedLink}}
                        <a href="{{formatUrl this.deployedLink}}" target="_blank" rel="noopener noreferrer" class="project-link">
                          <svg class="project-icon icon-external" viewBox="0 0 24 24">
                            <path d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
                          </svg>
                          Live Demo
                        </a>
                      {{/if}}
                    </div>
                  </div>
                  
                  {{#if this.techStack}}
                    <p class="tech-tag">
                      <span class="tech-tag-label">Technologies:</span> {{this.techStack}}
                    </p>
                  {{/if}}
                  
                  <div class="item-description">{{{this.projectSummary}}}</div>
                </div>
              {{/each}}
            </div>
          </div>
        {{/if}}
        
        <!-- Certifications Section -->
        {{#if certifications.length}}
          <div class="section">
            <h3 class="section-title">CERTIFICATIONS</h3>
            <div class="timeline-items">
              {{#each certifications}}
                <div class="timeline-item">
                  <div class="timeline-bullet"></div>
                  
                  <div class="item-flex-row">
                    <h4 class="item-title">{{this.name}}</h4>
                    {{#if this.date}}
                      <span class="item-date">{{this.date}}</span>
                    {{/if}}
                  </div>
                  
                  <div class="item-flex-row">
                    <h5 class="item-subtitle">{{this.issuer}}</h5>
                    {{#if this.credentialLink}}
                      <div class="contact-item">
                        <svg class="project-icon icon-link" viewBox="0 0 24 24">
                          <path d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101" />
                          <path d="M10.172 13.828a4 4 0 005.656 0l4-4a4 4 0 10-5.656-5.656l-1.102 1.101" />
                        </svg>
                        <a href="{{formatUrl this.credentialLink}}" target="_blank" rel="noopener noreferrer" class="contact-link">View</a>
                      </div>
                    {{/if}}
                  </div>
                  
                  {{#if this.description}}
                    <p class="item-description">{{this.description}}</p>
                  {{/if}}
                </div>
              {{/each}}
            </div>
          </div>
        {{/if}}
      </div>
      
      <!-- Right Column - Narrower for skills, education, etc. -->
      <div class="side-column">
        <!-- Skills Section -->
        {{#if skills.length}}
          <div class="section">
            <h3 class="section-title">SKILLS</h3>
            
            <div class="skills-container">
              {{#each skills}}
                <span class="skill-tag">{{this.name}}</span>
              {{/each}}
            </div>
          </div>
        {{/if}}
        
        <!-- Education Section -->
        {{#if education.length}}
          <div class="section">
            <h3 class="section-title">EDUCATION</h3>
            
            <div class="timeline-items">
              {{#each education}}
                <div class="project-item">
                  <h4 class="item-title">
                    {{this.degree}}{{#if this.major}}{{#if this.degree}} in {{/if}}{{this.major}}{{/if}}
                  </h4>
                  <h5 class="item-subtitle">{{this.universityName}}</h5>
                  
                  <div class="item-flex-row">
                    <span class="item-date">
                      {{this.startDate}}{{#if this.startDate}}{{#if this.endDate}} - {{this.endDate}}{{/if}}{{/if}}
                    </span>
                    {{#if this.grade}}
                      <span class="item-date">
                        {{this.gradeType}}: {{this.grade}}
                      </span>
                    {{/if}}
                  </div>
                  
                  {{#if this.description}}
                    <p class="item-description">{{this.description}}</p>
                  {{/if}}
                </div>
              {{/each}}
            </div>
          </div>
        {{/if}}
      </div>
    </div>
  </div>
</body>
</html>